node_modules/
dist/
.wrangler/
.dev.vars
.env
*.local
worker-configuration.d.ts
node_modules/.tmp/
# D1 dumps from `npm run db:export*` — full dumps carry analytics player_ids,
# and a restore point is a local artifact, not repo content.
backups/
